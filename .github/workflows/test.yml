name: test

on:
  push:
    branches: [master]
    paths-ignore:
      - 'docs/**'
      - 'README.md'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'README.md'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
      - uses: actions/checkout@v7
      - name: Install test dependencies
        run: pacman -Syu --noconfirm shellcheck python
      - name: Check Bash syntax
        run: bash -n get-arch lib/*.sh modules/*.sh tests/*.sh tests/run scripts/* archiso/get-arch-install
      - name: Check Python syntax
        run: python -m py_compile archiso/get-arch-disk-config tests/test_disk_config.py
      - name: Run ShellCheck
        run: shellcheck get-arch lib/*.sh modules/*.sh tests/*.sh tests/run scripts/* archiso/get-arch-install
      - name: Run tests
        run: ./tests/run
      - name: Exercise full check mode
        run: printf 'ciuser\nci-host\n' | ./get-arch --check
